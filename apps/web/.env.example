# Leave empty in local development so Vite uses its /api dev proxy.
VITE_API_ORIGIN=

# Used by the hosted frontend as a fallback when VITE_API_ORIGIN cannot be reached.
VITE_LOCAL_API_ORIGIN=http://127.0.0.1:3100

# GitHub Pages production build path.
VITE_BASE_PATH=/Booster-Break/
